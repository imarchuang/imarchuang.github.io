# Example idea (Markdown)

This page is the Markdown dump type. Docsify renders it at:

`https://imarchuang.github.io/#/ideas/example`

Copy `docs/ideas/example.md` to `docs/ideas/<slug>.md`, then add a line under **Notes** in [ideas/index](./index).

## Scratch

- One thought per bullet is enough.
- Public repo: no tokens, no customer data.

```text
docs/ideas/<slug>.md  →  /#/ideas/<slug>
```

Native HTML lives elsewhere: [example drop](/drops/example/ ':ignore').
